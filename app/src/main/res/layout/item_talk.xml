<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="88dp"
    android:background="?android:attr/selectableItemBackground"
    android:paddingBottom="@dimen/margin"
    android:paddingLeft="@dimen/margin_2x"
    android:paddingRight="@dimen/margin_2x"
    android:paddingTop="@dimen/margin">

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_above="@+id/tv_pub_date"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:layout_toLeftOf="@+id/iv_thumbnail"
        android:layout_toStartOf="@+id/iv_thumbnail"
        android:ellipsize="end"
        android:maxLines="3"
        android:textColor="@color/text_black"
        android:textSize="@dimen/body"
        tools:text="Alice Goffman: How we're priming some kids for college — and others for prison. Alice Goffman: How we're priming some kids for college — and others for prison" />

    <TextView
        android:id="@+id/tv_pub_date"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true"
        android:textColor="@color/text_grey"
        android:textSize="@dimen/caption"
        tools:text="01 May 2015" />

    <ImageView
        android:id="@+id/iv_thumbnail"
        android:layout_width="112dp"
        android:layout_height="match_parent"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="@dimen/margin"
        android:src="@color/accent" />

    <TextView
        android:id="@+id/tv_duration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_alignParentEnd="true"
        android:layout_alignParentRight="true"
        android:layout_marginRight="@dimen/margin"
        android:textColor="@color/text_white"
        android:textSize="@dimen/caption"
        tools:text="00:16:04" />
</RelativeLayout>